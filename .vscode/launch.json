{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug main (lldb)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/main",
      "args": [],
      "cwd": "${workspaceFolder}",
      "stopOnEntry": false,
      "preLaunchTask": "CMake Build",
      "terminal": "integrated"
    }
  ]
}
